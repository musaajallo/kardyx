// Shared component library — uses NativeWind on mobile and Tailwind on web.
// Components added here render identically across both platforms.
//
// Phase 1 first components: Button, Card, Input, Dialog (web), then
// game-specific: PowerUpBadge, CardFace, ScanFrame, PartyAvatar.

export const PLACEHOLDER = '@kardyx/ui — populated in Phase 1.';
